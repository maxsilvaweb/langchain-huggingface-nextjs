[pytest]
pythonpath = .
markers =
    langsmith: calls LLMs and uploads results to LangSmith (requires LANGSMITH_API_KEY)
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:
